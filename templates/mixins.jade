mixin author(authorName)
  - var author = contents.authors[authorName + '.json']
  p.author
    span.by 
    if author
      a(href='mailto:'+author.metadata.email)= author.metadata.name
    else
      =authorName

mixin date(longDate)
  - var i = longDate.getMonth()
  - var months = ['January', 'February', 'March', 'April', 'May', 'June', 'July', 'August', 'Septmber', 'October', 'November', 'December']
  - var month = months[i]
  - var date = longDate.toUTCString()
  - var prettyDate = parseInt(date.substr(5, 2)) + ' ' + month + ' ' + date.substr(12,4)
  - var showDate = (longDate.getTime() != new Date(0).getTime())
  if showDate
    p.date= prettyDate

mixin archive-date(longDate)
  - var date = longDate.toUTCString()
  - var prettyDate = parseInt(date.substr(5, 2)) + ' ' + date.substr(8, 3)
  span.archive-date= prettyDate

mixin tags(tagString)
  if tagString
    - var tagsarray = _.toArray(tagString.split(','))
    ul.post-categories
      each tag in tagsarray
        li= tag

mixin more(article)
  if article.hasMore
    p
      a(href=article.url).more Continue...

mixin google_analytics(account)
  script(type='text/javascript').
    (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
    (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
    m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
    })(window,document,'script','//www.google-analytics.com/analytics.js','ga');
    
    ga('create', '#{account}', 'tullo.ch');
    ga('send', 'pageview');

mixin mathjax
  script(type="text/x-mathjax-config").
    MathJax.Hub.Config({      
      tex2jax: { inlineMath: [['$','$'],['\\(','\\)']] },
      TeX: { equationNumbers: {autoNumber: "AMS"} }
    });
  script(type="text/javascript", src="http://cdn.mathjax.org/mathjax/latest/MathJax.js?config=TeX-AMS_HTML")
      
mixin header(locals)
  div.container
    div.gravatar
      img(src="http://www.gravatar.com/avatar/" + locals.gravatar + "?s=200").gravatar
    div#brand
      h1.site-title
        a.blog-title(href=locals.url)= locals.name
         — 
        span= locals.subtitle
    nav(role="navigation").site-navigation.main-navigation
      a(href="/about/") About
      |  | 
      a(href="https://github.com/ajtulloch") GitHub
      |  | 
      a(href="/static/cv.pdf") CV
   div.clear


